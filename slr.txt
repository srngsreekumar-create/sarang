#scatter plot
plot(x=mtcar$wt,y=mtcars$mpg,
     main="car weight vs fuel efficincy",
     xlab="weight(1000 lbs",
     ylab="miles per gallon(mpg0",
     col="red,pch=19")

#correlation test
corr_test=cor.test(mtcars$wt,mtcars$mpg,
                   method="pearson")
print(corr_test)

#residual analysis
r_val=(corr_test$estimate,)
r_val

#regression coefficients
cars
s=cars$speed
s_bar=mean(s)
s_bar

d=cars$dist
d_bar=mean(d)
d_bar

r=cor(dist,speed)
r

x=sd(s)
x

y=sd(d)
y

b1=r*(y/x)
b1

b0=d_bar-b1*s_bar
b0

plot(cars$speed,cars$dist,
     ylab="car stopping distnace",
     xlab="speed",
     main="car speed vs stopping distnace")

SLR=lm(dist~speed)
SLR

#normality assumption of the error term
residual=SLR$residuals
residual

#homoscedasticity assumption
plot(SLR$residuals~speed)
abline(0,0)

# R^2 and goodness fit
summary(SLR)

#hypothesis test for the significance of slope
anova(SLR)
lin_Mod=lm(dist~speed,data=cars)
lin_Mod

new_speeds=data.frame(speed=c(13,20,25,10))
new_speeds

predict(lin_Mod,newdata = new_speeds)
predict(lin_Mod,newdata = new_speeds,interval="prediction")
mydata=cars
library("ggplot2")
p=ggplot(mydata,aes(speed,dist))+
  geom_point()+
  stat_smooth(method=lm)
p
p+geom_line(aes(y=lwr),col="red",linetype="dashed")+
  geom_line(aes(y=upr),col="red",linetype="dashed")

mtcars
names(mtcars)
summary(mtcars)

model=lm(mpg~cyl+disp+hp+drat+wt+qsec+vs+am+gear+carb,data=mtcars)
model

teststat=coef(summary(model))[3,1]/
  coef(summary(model))[3/2]
teststat

2*pt(teststat,21,lower.tail=FALSE)

plot(model$residuals~mtcars$disp)
abline(0,0)

model2=lm(mpg~qsec+wt+am,data = mtcars)
summary(model2)

new_values=data.frame(wt=2.92,qsec=20.1,am=1)
new_values

x=predict(model2,new_values,interval="prediction")
x

y=predict(model2,new_values,interval="confidence")
y
